// 待解决！！！！！
// 树的常规操作：遍历打印
// 打印分类树
// STL: 收尾链接哪个效率高？
queue<vector<Node*>> BSTSequence(Node* bst){
    queue<vecotr<Node*>> res;
    queue<Node*> curLayer;
    curLayer.push(bst);
    while(curLayer.size() > 0){
        
    }
}

vector<Node*> Sequences(Vector<>)
